This assignment taught me how a module (such as bill.py) keeps the logic seperate from the console, notebook, and streamlit. I learned that if I import the functions they can keep all the math seperate. Then I also learned how to use the testing side tab to test my code and ensure that the imported functions are used properly. We saw this play out when the split_evenly function worked correctly but got handed the wrong argument so it would fail the test.  When test_grand_total failed I learened how to read the assertion error and use the debugger to help me out instead of just guessing. I also learned how breakpoints can be helpful when debugging to quickly test a line of code. What I want to work on for next time is telling unit and integration errors apart better. Next time a test fails I want to ask first whether the function itself is wrong or if it is being called with a wrong argument and use the varianles panel to confirm before I change any code.