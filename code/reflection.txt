This assignment taught me how a module (such as bill.py) keeps the logic separate from the console, notebook, and Streamlit interfaces. I learned that if I import the functions, they can keep all the math separate. Then I also learned how to use the Testing panel to test my code and ensure that the imported functions are used properly. We saw this play out when the split_evenly function worked correctly but got handed the wrong argument, so it failed the integration test even though every unit test still passed. When test_grand_total failed I learned how to read the assertion error and use the debugger to help me out instead of just guessing. I also learned how breakpoints can be helpful when debugging to inspect variables at a specific line of code. What I want to work on for next time is telling unit and integration failures apart better. Next time a test fails I want to ask first whether the function itself is wrong or whether it is being called with a wrong argument, and use the Variables panel to confirm before I change any code.